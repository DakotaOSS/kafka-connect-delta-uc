# Normalize to LF in the repo and on checkout everywhere, so line endings don't churn across platforms.
* text=auto eol=lf

# Shell scripts must stay LF — CRLF breaks bash.
*.sh text eol=lf

# Binary assets: never normalize.
*.png binary
